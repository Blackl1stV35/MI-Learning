# venv: .venv-train
# python -m venv .venv-train && .venv-train\Scripts\activate
# pip install -r requirements-train.txt
#
# CONFLICT NOTES:
#   numpy must be ==1.26.4 — torch 2.4 + cu121 ABIs break with numpy 2.x
#   do NOT pip install numpy separately after torch; let torch pin it
#   scipy must be >=1.11 — labels.py uses scipy.signal.lfilter
#   polars and duckdb have no conflicts with torch — safe to float versions
#   wandb is optional — comment out if not tracking experiments

torch==2.4.0+cu121
--extra-index-url https://download.pytorch.org/whl/cu121

numpy==1.26.4
scipy>=1.11.0,<2.0.0
polars>=0.20.0
pyarrow>=14.0.0
duckdb>=0.9.0
scikit-learn>=1.3.0
loguru>=0.7.0
tqdm>=4.66.0
python-dotenv>=1.0.0
wandb>=0.16.0
